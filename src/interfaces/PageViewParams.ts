export interface PageViewParams {
  userHash: string;
  hostname: string;
  websiteId: string;
  referer: string;
  browser: string;
  uniqueHash: boolean;
  time: string;
  countryCode: string;
  page: string;
}
