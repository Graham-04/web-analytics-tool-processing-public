export interface IncomingMessage {
  hostname: string;
  user_agent: string;
  user_hash: string;
  referer: string;
  ip_addr: string;
  website_id: string;
  country_code: string;
  page: string;
}
